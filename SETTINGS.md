# Sushidata Plugin Settings

These settings are instance-specific and should NOT be overwritten when updating skill files.

## API Configuration

**BASE_URL**: `https://dashboard.sushidata.ai/public/<YOUR_PUBLIC_URL_ID>/`

> ⚠️ **Setup required:** If `BASE_URL` still contains `<YOUR_PUBLIC_URL_ID>`, ask the user: _"What is your Sushidata dataspace ID? You can find it in your dashboard URL after `/public/`."_ Then substitute it before making any API calls.

**Tenant**: `Thru Content Marketing`

**Dataspace**: `Thru Content Marketing Internal`
